/**
 * App shell stub. M7 replaces this with the full one-question-at-a-time wizard (start →
 * answer → edit → lookup-status badge → retry → complete → summary), built on the API client.
 */
export default function App() {
  return (
    <main style={{ fontFamily: 'system-ui, sans-serif', padding: '2rem', maxWidth: 640 }}>
      <h1>Onboarding</h1>
      <p>Scaffold ready. The wizard UI is built in M7.</p>
    </main>
  );
}
